{
  "name": "triangle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "browserify src/WebGL.js -o build/main.js -t [ babelify --presets [ es2015 ] ]",
    "debug": "browserify src/WebGL.js -o build/main.js -t [ babelify --presets [ es2015 ] ] -d",
    "watch": "watchify src/WebGL.js -o build/main.js -dv"
  },
  "author": "jokly",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "gl-matrix": "^2.3.2",
    "watchify": "latest"
  }
}
